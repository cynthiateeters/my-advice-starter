{
  "name": "my-advice",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/js/ --ext .js --no-error-on-unmatched-pattern",
    "format": "prettier --write .",
    "test": "vitest",
    "prepare": "husky"
  },
  "lint-staged": {
    "src/js/**/*.js": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.4.2",
    "vite": "^7.3.1",
    "vitest": "^3.1.1"
  }
}
